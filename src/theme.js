import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#0a1931',
    },
    secondary: {
      main: '#185adb',
    },
    light: {
      main: '#f4f9f9',
    },
  },
});

export default theme;
